Техническое задание: Система мониторинга лог-файлов в реальном времени

1. Общее описание

1.1. Назначение
Система предназначена для непрерывного мониторинга лог-файлов в указанной директории на сервере под управлением Windows или Linux. При обнаружении новой строки, содержащей подстроку "ERR" (регистронезависимо), система регистрирует ошибку и отправляет уведомление через UI, email и/или Telegram. Уведомление не должно отправляться повторно для уже обработанных строк.

1.2. Основные требования
- Поддержка Windows и Linux.
- Отслеживание новых строк в лог-файлах в реальном времени.
- Поддержка масок имён файлов (например: *.log, error_*.txt).
- Игнорирование уже обработанных строк.
- Хранение данных в PostgreSQL через Entity Framework.
- Backend на C# (.NET 8).
- UI на React (TypeScript) с отображением уведомлений в реальном времени.
- Отправка уведомлений по email (через SMTP) и в Telegram (через бота).
- Модульная, масштабируемая и поддерживаемая архитектура.

2. Архитектура системы

Компоненты:
- Backend API: .NET 8 (C#), ASP.NET Core
- Database: PostgreSQL
- File Watcher: .NET FileSystemWatcher + кастомная "tail"-логика с поддержкой масок
- Notification Service: модуль отправки уведомлений (UI, Email, Telegram)
- Frontend UI: React 18 + TypeScript
- Realtime: SignalR или Server-Sent Events (SSE)
- Deployment: Docker (опционально), systemd / Windows Service

3. Технологический стек

3.1. Backend (C#)
- Язык: C# 12
- Framework: .NET 8 (LTS)
- ORM: Entity Framework Core 8
- База данных: PostgreSQL 14+
- NuGet-пакеты:
  - Npgsql.EntityFrameworkCore.PostgreSQL
  - Microsoft.EntityFrameworkCore.Tools
  - Microsoft.AspNetCore.SignalR
  - MailKit (для SMTP)
  - Serilog
  - System.IO.FileSystem

3.2. Frontend (React)
- Язык: TypeScript
- Библиотека: React 18
- State Management: Context API или Zustand
- Realtime: SignalR Client или SSE
- UI: MUI / Ant Design (по выбору)

3.3. База данных
- СУБД: PostgreSQL 14+
- Таблицы: Errors, Notifications

4. Структура базы данных

4.1. Таблица Errors
CREATE TABLE Errors (
    Id SERIAL PRIMARY KEY,
    File_Name TEXT NOT NULL,
    Content TEXT NOT NULL,
    Line_Position BIGINT NOT NULL,
    Created_At TIMESTAMPTZ DEFAULT NOW()
);

4.2. Таблица Notifications
CREATE TABLE Notifications (
    Id SERIAL PRIMARY KEY,
    Error_Id INT NOT NULL REFERENCES Errors(Id),
    Sent_At TIMESTAMPTZ DEFAULT NOW(),
    Is_Read BOOLEAN DEFAULT FALSE,
    Email_Sent BOOLEAN DEFAULT FALSE,
    Telegram_Sent BOOLEAN DEFAULT FALSE
);

5. Логика работы приложения

5.1. Инициализация
- Пользователь указывает:
  - Путь к директории с логами.
  - Маску файлов (например: "*.log", "app_*.txt").
- Сервис запускается как фоновый процесс.
- Сервис сканирует директорию и регистрирует файлы, соответствующие маске.

5.2. Отслеживание файлов
- Используется гибрид: FileSystemWatcher + кастомная логика чтения "с хвоста".
- Поддержка нескольких масок (через конфигурацию в виде массива).
- Для каждого файла сохраняется позиция последнего прочитанного байта.
- При изменении файла читается только новая часть данных.

5.3. Обнаружение ошибок
- Каждая новая строка проверяется на наличие "ERR" (IgnoreCase).
- Если найдено — вычисляется уникальный идентификатор (хеш или позиция).
- Проверяется дубликат в таблице Errors.
- При новой ошибке:
  - Запись в Errors.
  - Создаётся запись в Notifications (Email_Sent = false, Telegram_Sent = false).
  - Отправляется уведомление:
    - В UI (через SignalR/SSE).
    - По email (если настроен SMTP).
    - В Telegram (если настроен бот).

5.4. Дедупликация
- Уже обработанные строки не вызывают повторных уведомлений.
- Позиции чтения сохраняются в памяти (с fallback в БД при рестарте).
- При ротации логов (переименовании/удалении) позиция сбрасывается.

6. Уведомления (Email и Telegram)

6.1. Email (SMTP)
- Конфигурация в appsettings.json:
  {
    "Smtp": {
      "Host": "smtp.gmail.com",
      "Port": 587,
      "EnableSsl": true,
      "Username": "user@example.com",
      "Password": "******",
      "FromEmail": "alerts@company.com",
      "ToEmails": ["admin1@company.com", "admin2@company.com"]
    }
  }
- Используется библиотека MailKit.
- Тема письма: "[LOG MONITOR] Ошибка в {File_Name}"
- Тело: содержит Content, Timestamp, путь к файлу.

6.2. Telegram
- Конфигурация в appsettings.json:
  {
    "Telegram": {
      "BotToken": "123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11",
      "ChatId": "-1001234567890"
    }
  }
- Отправка через HTTP-запрос к Telegram Bot API: https://api.telegram.org/bot{token}/sendMessage
- Формат сообщения:
```
🚨 Новая ошибка в логе!
Файл: {File_Name}
Время: {Timestamp}
Содержимое:
{Content}
```

6.3. Обработка ошибок отправки
- При сбое отправки email/Telegram — логирование ошибки и повторная попытка (до 3 раз с экспоненциальной задержкой).
- Статус отправки обновляется в таблице Notifications после успешной доставки.

7. API-спецификация (Backend)

7.1. REST API (опционально)
- GET /api/errors — список ошибок (с пагинацией)
- GET /api/notifications — список уведомлений
- POST /api/configure — задать путь, маски, email/telegram (админка)

7.2. Realtime API
- Вариант A: SignalR Hub (ErrorNotificationHub)
- Вариант B: SSE — эндпоинт /api/events

7.3. DTO для уведомления
{
"FileName": "string",
"Content": "string",
"Timestamp": "datetime"
}

8. Фронтенд (React UI)

8.1. Функциональность
- Отображение уведомлений в реальном времени (всплывающие toast-сообщения).
- Список всех ошибок и уведомлений с фильтрацией.
- Возможность пометить уведомление как прочитанное.
- (Опционально) настройка пути и масок файлов через UI.

8.2. Подключение к realtime
- При запуске UI подключается к SignalR/SSE.
- Полученные события отображаются немедленно.

9. Деплой и эксплуатация

9.1. Backend
- Работает как Windows Service или systemd-юнит.
- Конфигурация: appsettings.json (LogDirectory, FileMasks, Smtp, Telegram, ConnectionStrings).

9.2. Frontend
- Собирается в статику (npm run build).
- Раздаётся через nginx или встроенный сервер .NET.

9.3. Мониторинг
- Внутреннее логгирование через Serilog (в файл и консоль).
- Health-check: GET /health → {"status": "ok"}

10. Безопасность
- SMTP-пароли и Telegram-токен — через переменные окружения или секреты (не в коде!).
- API защищён (JWT или API Key при необходимости).
- CORS настроен только для доверенных источников.

11. Тестирование
- Unit-тесты: дедупликация, парсинг, EF Core, маски файлов.
- Интеграционные тесты: файловый watcher, email/telegram-отправка (mock).
- E2E: проверка сквозного сценария от лога до уведомления.

12. Расширяемость
- Легко добавить новые каналы уведомлений (Slack, Teams и др.).
- Поддержка регулярных выражений вместо "ERR".
- Возможность настройки критичности (WARN, FATAL и т.д.).

13. Итоговая схема работы

[Log Files (по маскам)]
   ↓ (чтение новых строк)
[File Watcher + Tail Service]
   ↓ (поиск "ERR")
[Error Detection Engine]
   ↓ (если новая ошибка)
[PostgreSQL: Errors + Notifications]
   ↓
[Notification Router]
   ├──→ [UI (SignalR/SSE)]
   ├──→ [Email (SMTP)]
   └──→ [Telegram (Bot API)]