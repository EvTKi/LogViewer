# 🏗️ Архитектура LogMonitor

## 🔍 Обзор системы

LogMonitor — это **real-time система мониторинга лог-файлов**, предназначенная для обнаружения критических ошибок, предотвращения дублирования и мгновенного оповещения операторов через веб-интерфейс и Telegram.

Система построена по **слоистой (onion/hexagonal) архитектуре**, с чётким разделением ответственности:
```
[Frontend / SignalR Client]  
        ↑↓  
[LogMonitor.API] — Web-слой (presentation)  
        ↑↓  
[LogMonitor.Core] — Ядро (domain + contracts)  
        ↑↓  
[LogMonitor.Infrastructure] — Инфраструктурный слой (data, external services)
```

## 🧩 Ключевые компоненты

### 1. **LogMonitor.Core** — Ядро (чистая архитектура)

- **Сущности**: `ErrorEntity`, `NotificationEntity`, `FilePositionEntity`, `TelegramSubscriberEntity`
- **DTO**: `ErrorDto`, `NotificationDto`, `ConfigureRequest`
- **Интерфейсы**:
    - `IFileMonitoringService`
    - `IErrorDetectionService`
    - `INotificationRouter`
- **Конфигурации**: `TelegramOptions`
- **Не зависит** от внешних библиотек (чистый .NET)

### 2. **LogMonitor.Infrastructure** — Инфраструктура

- **Реализации сервисов**:
    - `HybridFileWatcher` — чтение файлов с отслеживанием позиции
    - `ErrorDetectionService` — проверка строки на `ERR`
    - `TelegramService` — отправка в Telegram
    - `TelegramPollingService` — приём `/start`
- **Доступ к данным**:
    - `LogMonitorDbContext` (EF Core + Npgsql)
    - Таблицы: `Errors`, `Notifications`, `FilePositions`, `TelegramSubscribers`
- **Логирование**: интеграция с Serilog

### 3. **LogMonitor.API** — Web-слой

- **REST API**: контроллеры для `Errors`, `Notifications`, `Configuration`
- **SignalR Hub**: `ErrorNotificationHub` — real-time уведомления
- **Хостинг**: ASP.NET Core + `IHostedService`
- **Конфигурация**: `appsettings.json` + `appsettings.local.json`
- **Документация**: Swagger

* * *

## ⚙️ Жизненный цикл и поток данных

1. **Старт приложения**
    - Загружается конфигурация (`appsettings.json` + `.local.json`)
    - Инициализируется Serilog (вывод в файл + консоль)
    - Проверяется подключение к PostgreSQL
    - Регистрируются все сервисы (включая `TelegramPollingService`)
2. **Запуск фонового мониторинга**
    - `LogMonitoringHostedService.StartAsync()` → `HybridFileWatcher.StartMonitoringAsync()`
    - Читаются позиции файлов из `FilePositions`
    - Устанавливаются `FileSystemWatcher` на целевую директорию
3. **Обнаружение ошибки**
    - При изменении файла → `ProcessFileAsync()`
    - Новые строки читаются с последней позиции
    - Строка с `ERR` → проверка дубликатов по:
        - `(FileName, LinePosition)` — точное совпадение позиции
        - `ContentHash (SHA256)` — семантический дубликат
    - При уникальности → сохранение в БД
4. **Маршрутизация уведомления**
    - Вызывается `INotificationRouter.RouteErrorAsync(ErrorDto)`
    - **SignalR**: отправка всем подключённым клиентам через `IHubContext`
    - **Telegram**: отправка всем подписчикам из `TelegramSubscribers` (если включено)
5. **Подписка на Telegram**
    - `TelegramPollingService` каждые 2 сек опрашивает `getUpdates`
    - Сообщения `/start` → сохраняют `chat_id` в БД
    - Только `chat_id > 0` (личные чаты)

* * *

## 🔒 Безопасность и ограничения

### Конфиденциальность

- Секреты (пароль БД, `BotToken`) хранятся в `appsettings.local.json`
- Файл исключён из Git через `.gitignore`
- Поддержка переопределения через переменные окружения (`CONNECTIONSTRINGS__DEFAULTCONNECTION`, `TELEGRAM__BOTTOKEN`)

### Ограничения NTA (Непал)

- **Telegram API (`api.telegram.org`)** заблокирован → отправка недоступна локально
- **NuGet (`api.nuget.org`)** частично недоступен → требуется явное указание версий пакетов (`8.0.0`)
- Решение: логирование ошибок без падения, развёртывание на внешнем сервере

### Отказоустойчивость

- Повтор отправки Telegram: до 3 попыток с экспоненциальной задержкой
- Обработка ошибок файловой системы: логирование, продолжение работы
- Дедупликация: защита от спама при ротации логов

* * *

## 📦 Зависимости

| Компонент | Пакет | Версия | Причина |
| --- | --- | --- | --- |
| EF Core | `Microsoft.EntityFrameworkCore` | `8.0.0` | Работа с PostgreSQL |
| Npgsql | `Npgsql.EntityFrameworkCore.PostgreSQL` | `8.0.0` | Драйвер БД |
| Serilog | `Serilog.AspNetCore` | `8.0.0` | Файловое логирование |
| SignalR | Встроено в ASP.NET Core | — | Real-time уведомления |
| Telegram | `HttpClient` | — | Отправка через REST API |

> 
> Все версии зафиксированы на `8.0.0` для совместимости и обхода NTA-проблем.

* * *

## 🌐 Сетевые взаимодействия

<svg width="1em" height="1em" fill="currentColor" aria-hidden="true" focusable="false" class=""><use xlink:href="#icon-line-download-02"></use></svg>

| Направление | Эндпоинт | Протокол | Доступность в Непале |
| --- | --- | --- | --- |
| Клиент → API | `http://localhost:5000` | HTTP/SignalR | ✅ |
| Приложение → PostgreSQL | `localhost:5432` | TCP | ✅ |
| Приложение → Telegram | `https://api.telegram.org/bot{token}` | HTTPS | ❌ (заблокирован NTA) |

* * *

## 📈 Масштабируемость (future-proof)

- **Поддержка группового мониторинга**: достаточно добавить `ChatId < 0` в логику подписки
- **Email/Slack**: легко добавить через `INotificationRouter`
- **Docker**: структура проекта готова к контейнеризации (`docker-compose.yml` в планах)
- **Метрики**: можно интегрировать с Prometheus через `Microsoft.Extensions.Diagnostics`